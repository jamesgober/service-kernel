# Custom


# Development
/dev
/.dev

# Private (not for GitHub)
/.private/

# Rust/Cargo Build Artifacts
/target/
**/*.rs.bk

# Dependencies
/node_modules/
/vendor/

# Editor & OS Junk
/.vscode
/.idea
.DS_Store
.Spotlight-V100
.Trash-*
.Trashes
.directory
.LSOverride
.AppleDouble
Desktop.ini
ehthumbs.db
Thumbs.db
$RECYCLE.BIN/

# Environment & Secrets
.env
/.claude
/config/
*.key
Homestead.yaml
Homestead.json
/config.json

# Sublime Text & Other IDEs
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache
*.sublime-workspace
*.sublime-project
._*

# Cargo lock for libraries — committed for reproducible CI but
# this comment exists so future maintainers know the choice was
# deliberate. Remove this section if the policy ever flips.
# Cargo.lock
